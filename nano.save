 /*
             console.log('imagenames', imageName) // Access the Python list in the template
             let imageNames =  imageName ;  // Assuming python_image_list is the variable containing the Python list
     
             // Construct the base URL using window.location.host
             var baseUrl = window.location.protocol + '//' + window.location.host;
             console.log(baseUrl)
     
             // Loop through the list and create <img> tags
             for (var i = 0; i < imageNames.length; i++) {
                 var img = document.createElement('img');
                 img.src = baseUrl + '/media/selfies/' + imageNames[i];  // Replace with the actual path to your images
                 img.alt = imageNames[i];
                 document.getElementById('imageContainer').appendChild(img);
                }
                */
